version: '3'
services:
  db:
    container_name: db
    image: postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - repo_db:/data/repo_db
    restart: always

volumes:
  repo_db:
